{
  "name": "nola",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "typecheck": "pnpm -r typecheck",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "pnpm -r test",
    "check:terminology": "bash scripts/check-terminology.sh",
    "seed": "echo 'seed arrives with Supabase setup (Day 2, plan section 8)'"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "typescript": "^5.6.3"
  }
}
