/**
 * Eval harness — regression runner over per-workflow golden cases.
 * A workflow without goldens is a demo, not a product (plan section 11).
 * Harness lands with workflow #1; every workflow is born with ten goldens.
 */
console.log(
  "No golden cases yet. Goldens are created per workflow, starting with discharge-summary.",
);
